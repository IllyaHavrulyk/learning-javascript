Какие из перечисленных ниже alert выполнятся?(в коментарях відповідь)

Какие конкретно значения будут результатами выражений в условиях if(...)?

if (-1 || 0) alert( 'first' );//виконається, -1
if (-1 && 0) alert( 'second' );//не виконається, 0
if (null || -1 && 1) alert( 'third' );//виконається, 1